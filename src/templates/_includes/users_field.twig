{% import "_includes/forms" as forms %}

{% set id = id ?? 'field-users' %}

{{
    forms.elementSelectField({
        elements: value,
        id: id ?? 'field-users',
        name: name,
        elementType: 'craft\\elements\\User',
        label: 'Users'|t('emails'),
        modalSettings: {
            hideSidebar: true
        }
    })
}}